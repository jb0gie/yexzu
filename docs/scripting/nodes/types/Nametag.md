# Nametag

The `Nametag` node displays a label and health bar above an entity.

### `.label`: String

The text to display on the nametag. Default is `'...'`.

### `.health`: Number

The health value to display (0-100). Default is `100`.
